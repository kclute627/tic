import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        alignItems: "center",
        paddingTop: 120,
    },
    logo: {
        height: 150,
        maxWidth: "60%",
        resizeMode: "contain",
    },

    btn: {
        marginTop: 80,
    },
    btns: {
        marginBottom: 22,
    },
});

export default styles;
